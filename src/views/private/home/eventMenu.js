export function eventMenu() {
    
    document.addEventListener('click', (e) => {
        e.preventDefault();

        if (e.target.matches('btn-ingresos')) {

        } else if (e.target.matches('btn-gastos')){

        }else if (e.target.matches('btn-ahorros')) {

        }else if (e.target.matches('btn-perfil')){
            
        }

    });
}